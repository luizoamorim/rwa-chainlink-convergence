# ==========================================================================
# AUTOLOCK DEFI — CRE WORKFLOW
# ==========================================================================

name: auto-lock-defi

triggers:
  - id: http-trigger
    type: http
    version: 1.0.0-alpha

targets:
  - id: evm-target
    type: evm
    version: 1.0.0

# ==========================================================================
# STAGING (Tenderly Virtual TestNet)
# ==========================================================================
staging-settings:
  user-workflow:
    workflow-name: "auto-lock-defi-staging"
    # Required Capabilities for the RWA workflow to function
    capabilities:
      - "networking:http@1.0.0-beta.0"      # For DETRAN/FIPE Oracle Fetch
      - "blockchain:evm@1.0.0"             # For Tenderly Minting
  workflow-artifacts:
    workflow-path: "."
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

# ==========================================================================
# PRODUCTION (Future DON Deployment)
# ==========================================================================
production-settings:
  user-workflow:
    workflow-name: "auto-lock-defi-production"
    capabilities:
      - "networking:http@1.0.0-beta.0"
      - "blockchain:evm@1.0.0"
  workflow-artifacts:
    workflow-path: "."
    config-path: "./config.production.json"
    secrets-path: "../secrets.yaml"